import UIKit

extension UIColor {

    enum GraphicView {

        enum TableView {
            static var backgroundColor = UIColor.systemCyan
            static var separatorColor = UIColor(red: 248 / 255,
                                                green: 216 / 255,
                                                blue: 28 / 255,
                                                alpha: 1)
        }

        enum NavigationBar {
            static var tintColor = UIColor(red: 248 / 255,
                                           green: 216 / 255,
                                           blue: 28 / 255,
                                           alpha: 1)
        }
    }
}
